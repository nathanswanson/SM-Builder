apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: www
spec:
  type: ClusterIP
  selector:
    app: postgresql
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432