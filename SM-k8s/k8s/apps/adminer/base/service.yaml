apiVersion: v1
kind: Service
metadata:
  name: adminer-service
  namespace: www
spec:
  type: ClusterIP
  selector:
    app: adminer-deployment
  ports:
    - name: http
      port: 80
      targetPort: 8080
