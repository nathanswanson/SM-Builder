name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
  workflow_run:
    workflows: [Artifact Builder]
    types: [completed]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: server-manager

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
      
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: 'recursive'
      - name: Run Frontend Action
        uses: dawidd6/action-download-artifact@v11
        with:
          # name: server-manager-frontend
          github-token: ${{secrets.GITHUB_TOKEN}}
          repo: nathanswanson/SM-Frontend
  
      - name: Run Backend Action
        uses: dawidd6/action-download-artifact@v11
        with:
          # name: server-manager-backend
          github-token: ${{secrets.GITHUB_TOKEN}}
          repo: nathanswanson/SM-Backend
          
      - name: Registry Login
        uses: docker/login-action@v3
        with:
          registry: ${{env.REGISTRY}}
          username: ${{ github.actor}}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Metadata
        uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
      
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build and Push
        uses: docker/build-push-action@v6
        with:
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
